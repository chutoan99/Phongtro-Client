export const areaQuery = {
  query: `query {
            area {
            err
            msg
            response {
              code
              createdAt
              id
              order
              updatedAt
              value
            }
          }
  }`,
};
